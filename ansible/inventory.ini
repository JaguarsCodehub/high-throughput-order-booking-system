[ec2_servers]
# Replace this IP with your actual EC2 public IP if running from your laptop.
# If running Ansible directly ON the EC2 instance, you can use:
# localhost ansible_connection=local
obs-server ansible_host=YOUR_EC2_PUBLIC_IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/your-key.pem
